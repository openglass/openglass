intelligent search
export to excel
openglass as home page
for new protocol we can use existing tags ?
copy an existing tag and edit it and save it as a new tag
if you clikc on a column name it shoujld be sortable
